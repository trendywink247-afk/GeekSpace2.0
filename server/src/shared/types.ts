// ============================================================
// Shared type definitions used across all modules
// ============================================================

// Re-export AuthRequest so modules can import from shared/
export type { AuthRequest } from './middleware/auth.js';
