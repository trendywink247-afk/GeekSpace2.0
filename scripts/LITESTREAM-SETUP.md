# Litestream Setup

Moved to [`docs/LITESTREAM.md`](../docs/LITESTREAM.md) as of AGE-15.

The runbook now covers:

- Config files in `ops/litestream/`
- First-time activation via the `litestream-install` arm of
  `.github/workflows/ops-remote-exec.yml`
- Monitoring, restore drill, and disaster-recovery procedures
- Credential rotation and uninstall paths
