{
  "name": "geekspace-smoke-tests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "smoke": "tsx smoke-tests.ts",
    "test": "tsx smoke-tests.ts",
    "lint": "tsc --noEmit || echo 'No tsconfig.json'",
    "typecheck": "tsc --noEmit || echo 'No tsconfig.json'",
    "build": "echo 'No build step needed'"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
