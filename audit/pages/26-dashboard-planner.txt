PAGE: Planner
ROUTE: /dashboard/planner
STATUS: PLACEHOLDER

=== DESKTOP VIEW ===
- Single centered card with dark glassmorphism background
- "Coming Soon" badge (chartreuse/ADFF2F color with Sparkles icon)
- CalendarCheck icon (8x8, chartreuse) in a rounded square box
- "Weebo Task Planner" heading (lg, semi-bold)
- Description paragraph: "Plan complex multi-step workflows and let your Weebo fleet execute them autonomously. Break down goals into actionable tasks with AI-powered orchestration."
- No interactive elements; completely static

=== MOBILE VIEW ===
- Layout is simple flex column centered — renders correctly at all widths
- p-12 padding on the inner container: on very narrow screens (< 320px) this may be slightly cramped but otherwise fine
- Text is responsive and readable
- No touch targets needed as there are no interactive elements

=== FUNCTIONALITY CHECK ===
Calendar view — MISSING (not implemented)
List view — MISSING (not implemented)
Kanban view — MISSING (not implemented)
Create task/event — MISSING (no form)
Edit/delete task — MISSING
Date navigation — MISSING
Drag-and-drop — MISSING
Integration with reminders — MISSING
Backend wiring — MISSING (no API calls; no server route for planner tasks)
Any user interaction — MISSING

=== LOGIC ISSUES ===
1. This is a pure static placeholder. There are no imports of API services, no state, no useEffect calls, no data fetching of any kind.
2. The page imports only CalendarCheck and Sparkles from lucide-react — nothing else. No dashboardStore usage.
3. No error boundary or fallback state needed since nothing can fail.
4. No route guard — the page is reachable by any authenticated user and will always show the same Coming Soon content.
5. The description mentions "Weebo fleet" — verify this is consistent with the brand name policy (product name is "Agentin Chat"; "Weebo" is an agent persona which is allowed).

=== IMPROVEMENTS ===
Priority HIGH — Implement Planner with at minimum a list-view of tasks tied to reminders or a new tasks table. A "Coming Soon" placeholder provides zero value to active users.
Priority HIGH — Consider whether Planner should integrate with the existing reminders system (reuse the reminders DB table with a task-type flag) or introduce a separate tasks schema.
Priority MED — Add a simple calendar grid (month view) with reminders overlaid as a first phase, reusing existing reminder data without a new backend table.
Priority MED — Add a kanban board as a second phase: columns for Not Started / In Progress / Done, drag-and-drop reordering.
Priority LOW — Consider adding a "Notify me when available" subscribe button so users can be notified when Planner launches.
Priority LOW — If the feature timeline is far out, consider removing the sidebar nav item to avoid user confusion, or keep it but add an estimated release date.

=== SERVER ROUTES USED ===
None. This page makes no API calls and has no backend routes. No planner/tasks route file exists in /root/GeekSpace2.0/server/src/routes/.
