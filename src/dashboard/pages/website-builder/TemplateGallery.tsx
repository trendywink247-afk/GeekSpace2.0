import { TemplateGalleryPage } from '../TemplateGalleryPage';

/**
 * Thin wrapper that embeds the TemplateGalleryPage inside the Website Builder tab.
 */
export function TemplateGallery() {
  return <TemplateGalleryPage embedded />;
}
